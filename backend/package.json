{
  "name": "xionxepay",
  "version": "1.0.0",
  "description": "**Xionxepay** is a smart, Web3-powered Point-of-Sale (POS) system that allows vendors to accept stablecoin payments through QR codes — no crypto wallets or blockchain knowledge required.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "prettier": "prettier --write \"src/**/*.{ts,js,html,json,md}\"",
    "prettier:check": "prettier --check \"src/**/*.{ts,js,html,json,md}\"",
    "format": "npm run prettier && npm run lint:fix",
    "format:check": "npm run prettier:check && npm run lint",
    "start:dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "start:prod": "node dist/server.js",
    "build:prod": "tsc && cp src/.env dist/.env && cp src/package.json dist/package.json && cp src/README.md dist/README.md",
    "start": "node dist/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devprinceng/xionxepay.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/devprinceng/xionxepay/issues"
  },
  "homepage": "https://github.com/devprinceng/xionxepay#readme",
  "dependencies": {
    "@abstract-money/actions-xion": "^0.1.3",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.16.0",
    "nodemailer": "^7.0.3",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.0.3",
    "@types/nodemailer": "^6.4.17",
    "@types/qrcode": "^1.5.5",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
